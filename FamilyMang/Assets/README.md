# Assets

Положите сюда файл **`AtpTlpLogo.png`** (логотип ATP tlp для шапки окон каталога и загрузки).

Файл в `.gitignore` и в репозиторий не попадает. При сборке копируется в `bin\Debug\Assets\`.

Скрипт `Install-RevitAddin.ps1` также копирует логотип в папку Revit Addins рядом с DLL.
